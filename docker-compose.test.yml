version: '3.8'
services:
  test-mongo:
    image: mongo:7
    ports:
      - "27018:27017"
    tmpfs:
      - /data/db    # RAM-backed volume for speed in CI
    environment:
      - MONGO_INITDB_DATABASE=will_guide_test
